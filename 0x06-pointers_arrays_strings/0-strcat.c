#include "main.h"
#include <string.h>
/**
 * _strcat - this function concate to strings
 *
 * @n: is the pointer
 *
 * Return: Always 0
 */


char *_strcat(char *dest, char *src)
{
	unsigned int i;

	i = strlen(*dest) + strlen(*src);

	char *string;

	while ()
	{
	}
	return *dest
}
