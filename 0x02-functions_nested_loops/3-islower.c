#include "main.h"

/**
 * islower - Entry point
 *
 * Return: 0 or 1
 *
 * description: print lowercase alphabet
 */


int print_alphabet(int a)
{
	if (a >= 'a' && a <= 'z')
	{
		return (1);
	}
	return (1);
}
