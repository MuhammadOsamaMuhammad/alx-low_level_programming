#include "main.h"

/**
 * print_rev_ - function that returns print reverse string
 * @s: pointer
 * Return: void
 */

void print_rev(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{
	}
	while (i > 0)
	{
		_putchar(s[i]);
	}
}
